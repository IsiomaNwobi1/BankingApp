#spring.application.name=Banking-Application
# ===========================================================
# - DATASOURCE-PROPERTIES
# ===========================================================
spring.datasource.url=jdbc:mysql://localhost:3306/bankappdb
spring.datasource.username=${USERNAME}
spring.datasource.password=${PASSWORD}
spring.datasource.drive-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE


# ===========================================================
# - EMAIL-PROPERTIES
# ===========================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL}
spring.mail.password=${EMAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# ===========================================================
# - HIBERNATE-PROPERTIES
# ===========================================================
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.format_sql=true

# ===========================================================
# - CLOUDINARY
# ===========================================================
cloudinary.name=decackyfq
cloudinary.api.key=679654242588932
cloudinary.api.secret=${CLOUDINARY_SECRET_KEY}


# ===========================================================
# - JWT-PROPERTIES
# ===========================================================
app.jwt-secret=${JWT_SECRET}
app.jwt-expiration=86400000